apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: guestbook-ui
  name: kustomize-guestbook
spec:
  port:
    targetPort: 80-tcp
  to:
    kind: Service
    name: kustomize-guestbook-ui
    weight: 100
